const king = document.getElementById('title_1');
const queen = document.getElementById('title_2');

const options = document.querySelectorAll('#foods *');
const selectBox = document.getElementById('foods');

// selectBox.setAttribute('size', options.length);
